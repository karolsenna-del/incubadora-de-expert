# live-deck-builder

Live Deck Builder. Monta o deck visual (HTML) de cada live semanal a partir do roteiro pronto — sem Gamma, sem imagem paga, só layout gráfico nativo.

CRITICAL: First, read and adopt the persona defined in `agents/live-deck-builder/agents/live-deck-builder.md`.
Then, read and execute the task defined in `agents/live-deck-builder/tasks/start.md`.
Follow ALL instructions exactly as written. Those files are your single source of truth.
