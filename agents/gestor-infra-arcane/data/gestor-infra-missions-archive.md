# Gestor de Infra Arcane — Mission Log (Archive)

> Entradas arquivadas na revisao mensal de 2026-08-12 (primeira revisao do Mission Log).
> Motivo do arquivamento: ja cobertas por SOP/regra permanente em outro lugar, ou superadas por missao posterior.
> Historico preservado aqui — nao aparece mais na tabela principal (`gestor-infra-missions.md`).

---

| # | Data | Missao | Resultado | SOP Criado? | Observacoes | Motivo do arquivamento |
|---|------|--------|-----------|-------------|-------------|------------------------|
| 3 | 2026-08-02 | Replicar as outras 6 paginas (Metodo VIP, Sprint do Metodo, Grupo, Individual, Expert360, Diagnostico Ferramentas) seguindo o padrao do piloto (SOP-018) | ok | nao (reuso do SOP-018) | Todas as 7 paginas no ar em `https://vendas-incubadora.vercel.app/{slug}/`, testadas (200 OK) | Execucao rotineira reusando SOP-018, sem informacao nova |
| 4 | 2026-08-02 | Trocar CTA de Grupo/Individual/Expert360 do WhatsApp pro checkout real assim que a Karol passou os links (Voomp pro curso, Hotmart pras 2 mentorias) | ok | nao | Href trocado + evento de pixel `Contact` -> `InitiateCheckout` nessas 3 (mais correto pro tipo de acao). Verificado via curl que o href certo esta no ar em cada uma. Pendencia fechada | Ajuste pontual de conteudo ja concluido, sem padrao reutilizavel |
| 7 | 2026-08-11 | Adicionar icone flutuante de WhatsApp na pagina diagnostico-ferramentas (projeto vendas-incubadora), reaplicando o padrao do SOP-019 (mesmo CSS/HTML/JS de metodo-vip e sprint-do-metodo) | ok | nao (reuso do SOP-019) | CTAs principais da pagina continuam apontando pro WhatsApp (nao migraram pra checkout — fora de escopo desta missao), entao so o icone flutuante foi adicionado como reforco de canal de duvida, com mensagem contextual propria e pixel `Contact` no clique. Validado local (`node -c` no JS extraido) e em producao (curl 200 + grep 5x `whats-flutuante`) + visual via Playwright desktop (1366px) e mobile (390px): icone sobe (`.subiu`) junto com a barra mobile sem colidir | Execucao rotineira reusando SOP-019, sem informacao nova |
| 8 | 2026-08-11 | Consolidacao das 2 copias divergentes deste agente: `agents/gestor-infra/` (registrada como `/techOps`, com este Mission Log + Playbook reais/atualizados ate a missao #7) e `agents/gestor-infra-arcane/` (registrada como `/gestor-infra-arcane`, com Vault vazio e Mission Log/Playbook parcial — sem SOP-019/SOP-020, sem os creds Vercel/Apify) | ok | nao (consolidacao, nao processo novo) | Causa raiz: rebrand pra `gestor-infra-arcane` foi feito criando pasta nova em vez de renomear a existente — as duas ficaram registradas em paralelo (`.claude/commands/techOps.md` e `.claude/commands/gestor-infra-arcane.md`, ambas apontando pra pastas diferentes) e acumularam missoes reais e distintas cada uma. Resolucao: `agents/gestor-infra-arcane/` (a que tem entrada no registro `.agents/skills/`, entao a canonica) virou a pasta unica — Vault mesclado (Vercel + Apify, sem perda de credencial), Playbook mesclado (colisao de numero: SOP-018 antigo do `techOps` — "Publicar LP estatica na Vercel + subdominio" — renumerado pra SOP-020, ja que SOP-018 la significava outra coisa — "Implementar pagina de vendas a partir de copy aprovada"), Mission Log mesclado cronologicamente (7 missoes reais). `agents/gestor-infra/` deletada. `.claude/commands/techOps.md` atualizado pra apontar pra `agents/gestor-infra-arcane/` (alias, mesmo destino — preserva quem digita `/techOps` por habito). Nenhuma outra referencia no repo apontava pra pasta antiga (conferido via grep) | Consolidacao pontual ja resolvida, nao se repete |
| 9 | 2026-08-11 | Pesquisa (handoff do Companion): a Voomp expoe webhook/API de notificacao de venda? Contexto: Karol avaliando area de membros propria pra mentoria + Expert360º, quer saber se da pra manter o checkout Voomp Pay e so trocar a liberacao de acesso pra plataforma nova | ok | nao (achado documentado na KB, nao SOP) | CONFIRMADO: Voomp Creators tem webhook de vendas nativo (produto > aba Entregas > eventos Vendas/Assinatura/Checkout-abandonado + URL de callback), com integracao ja usada por Zapier/Make/Notazz/ActiveCampaign. E API diferente da `api.voompplay.com.br` que o course-publisher ja usa (essa e interna, so gestao de curso/aula). Documentado em `gestor-infra-kb.md` secao 1.3. GAP: nao testei o payload real (schema JSON) nem confirmei retry/idempotencia — precisa disparar venda de teste ou usar webhook.site antes de integrar de verdade | Pesquisa preliminar superada pela implementacao real do webhook (missao #27) |
| 16 | 2026-08-11 | **INCIDENTE:** Área de Membros usando projeto Supabase errado — e-mail de login chegou com marca "ARKA — Mentoria Arcane" pro aluno. Karol também pediu campo de código (o link sozinho não bastou) e alertou sobre validar e-mail contra alunos ativos | ok | sim (REGRA-015 + KB vault) | **Causa raiz:** na missão #9/#10 eu tinha reaproveitado a credencial Supabase achada via grep em `lib/supabase.js` (`tzvfkdqzdkftcqfourom.supabase.co`) sem checar `business/vault/supabase.md` primeiro — era projeto ERRADO (provavelmente da Arcane/Launch Command Center), não o "incubadora-de-expert" real (`pxnbcbhgoewrwyreohki`, confirmado no vault). Corrigido `js/config.js` pro projeto certo. **Verificado no projeto certo (Management API):** template de e-mail é o genérico da Supabase ("Your sign-in link", sem marca nenhuma — pelo menos não vaza mais Arcane), mas customizar assunto/remetente pra "Incubadora de Expert" não é possível no free tier sem SMTP customizado (API recusou com 400) — fica pendente, precisa a Karol decidir um provedor (sugestão: Resend, free tier bom). **Bug real achado e corrigido:** `site_url` do projeto certo estava em `http://localhost:3000` (nunca configurado) e `uri_allow_list` vazio — o redirect do magic link para produção provavelmente falharia silenciosamente. Corrigido via Management API pra `https://area-de-membros-incubadora.vercel.app`. **Pedido do código:** adicionado fluxo de 2 passos no login — pede e-mail, envia (Supabase manda link + código de 6 dígitos no mesmo e-mail por padrão), e agora tem campo pra digitar o código manualmente (`verifyOtp`) como alternativa ao clique no link — mais robusto também contra e-mail corporativo que escaneia/gasta o link sozinho. **Ponto da Karol sobre validar e-mail contra alunos ativos:** registrado como pendência de Fase 4 no tracker — hoje qualquer e-mail consegue criar conta (`shouldCreateUser` default da Supabase é true); vai precisar de checagem contra tabela de matrícula antes de liberar conteúdo de verdade. Regra nova criada: REGRA-015 (checar vault antes de reaproveitar credencial achada por grep). Validado local (JS syntax + Playwright, UI dos 2 passos do login) antes de deploy. Redeployado | Incidente ja 100% capturado permanentemente na REGRA-015 (`gestor-infra-rules.md`) — entrada redundante no log |
| 19 | 2026-08-11 | Deixar o botão de som mais convidativo (pulso visual) — Karol insistiu em autoplay com som, expliquei que é bloqueio de navegador (Chrome/Firefox/Safari, nenhum permite áudio automático sem gesto do usuário — não é limitação nossa, é política de todos os browsers desde ~2018 contra abuso de anúncio) | ok | nao (ajuste pontual) | Adicionado `animation: pulso-som` (anel pulsando 3x) no botão de som pra chamar atenção nos primeiros segundos, some depois do primeiro toque (`.ativado`). Não resolve o pedido literal (autoplay+som é tecnicamente impossível), mas aproxima o resultado prático — mais gente vai notar e tocar o botão. **Achado no mesmo momento:** Karol reportou receber só o link, sem código, no e-mail de login — investigado e confirmado: o template PADRÃO da Supabase (o único disponível, já que customização tá bloqueada no free tier — mesma limitação da missão #16) não inclui `{{ .Token }}`, só o link. Ou seja, o campo de código que constrói na tela de login (missão #16) tá funcionalmente OK mas PRATICAMENTE inútil — o código nunca chega no e-mail dela. Fix de verdade exige SMTP customizado (proposta: Resend, free tier) pra poder customizar o template e incluir o código — checei o vault (`business/vault/`) primeiro dessa vez (REGRA-015) e confirmei que não existe nenhum provedor de e-mail configurado ainda. Perguntei pra Karol se quer que eu configure — depende de conta nova + verificação de domínio dela, não é algo pra fazer sem perguntar | Problema resolvido de vez pela missao #20 (SMTP Resend) — entrada mantida so como historico do diagnostico |
| 28 | 2026-08-12 | Popular banco de frases motivacionais (2 PDFs em Downloads) + revisar copy do app pra masculino genérico (Karol tem 40% de alunos homens) | ok | nao (conteúdo/copy, não processo novo) | **Frases:** "Bom dia.pdf" (37MB) e "Bom dia 2.pdf" (23MB) excediam o limite de 20MB do Read tool (eram pôster/calendário, quase só imagem) — usei `pdftotext -layout` (poppler, já disponível no ambiente) pra extrair o texto por trás da arte. Encoding veio quebrado (acentos como `�`) e a ORDEM ficou embaralhada em vários trechos (pdftotext lê caixas de texto na ordem interna do PDF, não a ordem visual do pôster — comum em design gráfico com texto sobreposto/rotacionado). Reconstruí os acentos manualmente por frase (português correto, conhecimento próprio das citações clássicas) e associei frase↔autor pela adjacência no texto extraído quando ficou claro; descartei ~8 frases que vieram genuinamente ilegíveis/embaralhadas (não adivinhei) e uma palavra (Herzberg, "reconhecimento") ficou parcialmente inferida por contexto — sinalizada no comentário do código, não é extração 100% literal. Resultado: 84 frases em `FRASES_MOTIVACIONAIS` (data.js), substituindo os 4 placeholders. Puladas de propósito: textos de Natal (não evergreen), labels de design ("LEMBRETE!", "PLACE COUNTDOWN STICKER HERE"). Flagado pra Karol: tom das frases (citação corporativa clássica) é bem diferente da voz autoral que os placeholders tinham. **Gênero:** Karol interrompeu a sessão pra avisar que reparou "chamada"/"alunas" no popup de benchmarking e queria revisão geral (40% dos alunos são homens, nunca usou feminino como padrão antes — bate com preferência já registrada em memória). Varri `app.html`+`js/app.js`+`js/data.js` com grep por termos de gênero, separando o que refere à PESSOA (precisa virar masculino: "Bem-vinda"→"Bem-vindo", "chamada"→"chamado", "alunas"→"alunos", "colegas"→"colegos"... corrigido "outras colegas"→"outros colegas") do que concorda com um SUBSTANTIVO feminino da língua portuguesa (ex: "matrícula liberada", "consultoria liberada" — LEGÍTIMO ficar feminino, não é sobre a pessoa, é concordância gramatical; não alterei). Frases do banco motivacional (citações históricas) também não foram alteradas — são texto de terceiros, mudar descaracterizaria a citação. Validado local (`node -c` + Playwright modo preview, sem erro, screenshot confirmando "Bem-vindo de volta" e "Como você quer ser chamado"). **INCIDENTE no deploy:** sessão já rodando há muitas horas, cwd do Bash tinha se movido de comando pra comando — um `cd business/campanhas/.../site` relativo, rodado em cima de um cwd que já estava dentro de `site/`, empilhou um caminho aninhado bogus (`site/business/campanhas/.../site`). O `vercel --prod` rodou dentro desse diretório errado (sem link `.vercel/project.json` válido pro projeto real) e a Vercel, sem avisar nem dar erro, CRIOU um projeto novo do zero chamado "site" (nome da pasta) e publicou ali — só percebi porque a URL do deployment retornada era `site-xxxxx...` em vez de `area-de-membros-incubadora-xxxxx...`. Corrigido: `rm -rf` no diretório bogus, `vercel remove site --yes` (projeto stray removido sem deixar lixo), redeploy no diretório/projeto certo (confirmado ANTES via `pwd` + `cat .vercel/project.json`). Confirmado em produção via curl (grep no HTML/JS servido: "Bem-vindo de volta" presente, 76 linhas de frase com atribuição de autor). REGRA-016 criada na KB de regras pra sempre confirmar `pwd`/`.vercel/project.json` antes de `cd` relativo ou `vercel --prod` em sessão longa | Trabalho de frases revertido na missão #29 (resultado final nulo); revisão de gênero é ajuste de conteúdo já aplicado, sem padrão a reter; REGRA-016 (o unico ganho permanente) ja preservada no `gestor-infra-rules.md` |
| 29 | 2026-08-12 | Reverter o banco de 84 frases (missão #28) — Karol avaliou depois de eu dar minha opinião honesta (perguntada por ela) e concordou que estava desconexo do posicionamento atual | ok | nao (reversão + arquivamento) | Dei minha avaliação quando perguntada: as 84 frases eram citação de autoridade emprestada (Peter Drucker, Walt Disney), tom de fase anterior do negócio da Karol (público "empreendedor", não "especialista") — tensão direta com o próprio posicionamento do método ("Diploma é o que você estudou. Método é o que você viveu." é uma crítica a exatamente esse tipo de autoridade emprestada). Karol concordou e pediu reversão, mais arquivar o material bruto pra Aria (squad-conteudo-arcane) usar de referência quando for escrever o banco de verdade, sem precisar repetir a extração dos PDFs. Ações: criado `business/campanhas/area-de-membros/material-bruto-frases-standby.md` com as 84 frases + contexto de por que não foram usadas + os 4 exemplos de tom correto como referência pra quem for escrever depois; `FRASES_MOTIVACIONAIS` em `data.js` revertido pros 4 placeholders originais; validado (`node -c`) e publicado — confirmado via curl que o texto "Diploma é o que você estudou" voltou a aparecer em produção. Registrado no `demandas-backlog.md` do Companion (visibilidade cross-sessão, já que a Karol vai chamar a Aria num chat separado) e atualizado o tracker da Área de Membros. Segui a REGRA-016 (criada na missão anterior) antes desse deploy: confirmei `pwd` + `.vercel/project.json` antes de publicar — sem repetir o incidente | Resultado final da tentativa das frases (revertido pro estado original) — junto com #28, nao carrega mais informacao acionavel |

---

*Arquivo de arquivamento. Consultar apenas se precisar reconstruir historico detalhado de uma missao especifica.*
