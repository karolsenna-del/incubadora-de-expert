# Course Publisher — Estado da Sincronização Semanal (Meet → Área de Membros)
# Atualizado a cada execução da task weekly-sync.md — nunca editar manualmente
# sem necessidade, é o que evita subir aula duplicada.
# 14/08/2026: destino mudou de Hotmart (upload nativo) pra YouTube não-listado + tabela
# encontros_mentoria no Supabase — ver weekly-sync.md pro protocolo novo. Corte direto,
# sem período em paralelo (decisão da Karol).

live_expert360:
  ultimo_processado_titulo: "Live Expert360 - 2026/08/19 13:58 GMT-04:00 - Recording"
  ultimo_processado_data: "2026-08-19T19:10:58.082Z"  # createdTime do arquivo no Drive (Live 26)
  proximo_numero: 27
  ultima_execucao: "2026-09-03"
  # 03/09/2026 (2ª rodada, missão #22, disparada via /course-publisher + weekly-sync.md direto):
  # reconfirmado — sem gravação nova em nenhuma das duas raízes, próxima gravação esperada segue
  # sendo Live 27.
  # 03/09/2026 (missão #21, disparo autônomo REGRA-015): reconfirmado — sem gravação nova em
  # nenhuma das duas raízes, próxima gravação esperada segue sendo Live 27.
  # 27/08/2026 (2ª rodada, missão #20, mesmo dia): reconfirmado — sem gravação nova em nenhuma
  # das duas raízes, próxima gravação esperada segue sendo Live 27.
  # 27/08/2026 (missão #19): sem gravação nova em nenhuma das duas raízes (pasta clássica e
  # "Live Expert360 (recurring)") — próxima gravação esperada é Live 27.
  # 06/08/2026: a partir desta gravação, o Meet passou a salvar em pasta nova
  # "Live Expert360 (recurring)" (id 1GGeLSKWDPiKT-GNic7GeKyDIU4A8asAv), dentro de uma
  # pasta raiz nova "Google Meet" (id 1mlJbXrY5rLxcobA9PTztLcX0NnMXr17L) — ambas criadas em
  # 2026-08-05T18:41:47Z. Karol confirmou que não mexeu no convite recorrente do Calendar —
  # então é comportamento novo do lado do Google/Workspace, não algo que ela mudou. A pasta
  # antiga "Meet Recordings" segue existindo e pode voltar a ser usada — busca do weekly-sync
  # agora precisa checar as duas.
  # 13/08/2026: gravação de 12/08 encontrada (Live 25) mas NÃO upada — ver pendência abaixo.
  # 20/08/2026: gravação de 19/08 também encontrada (Live 26) — MESMO bloqueio, agora mais
  # grave (ver pendência "acesso_drive_bloqueado" abaixo). ultimo_processado_data/titulo NÃO
  # avançaram de propósito, pra próxima rodada reencontrar as duas e tentar de novo.
  # 20/08/2026 (2ª rodada, missão #16, mesmo dia): reconfirmado — nenhuma gravação adicional
  # além de Live 25 e 26, bloqueio de acesso ao Drive segue idêntico.
  # 20/08/2026 (missão #17): Karol resolveu o compartilhamento (ver pendência resolvida abaixo).
  # ACHADO CRÍTICO ao processar: Live 25 JÁ ESTAVA em encontros_mentoria (youtube_id 6gSyVQlWGR8,
  # inserida em 14/08 pelo backfill histórico das 25 Lives — processo separado que nunca
  # atualizou este state.yaml). Esse state ficou desatualizado desde 14/08, achando que Live 25
  # ainda precisava ser processada. Cheguei a subir um vídeo NOVO duplicado pra Live 25
  # (youtube_id uWdDRBNl2DY) antes de checar a tabela — DELETADO do YouTube assim que percebi
  # (nenhuma linha duplicada chegou a entrar no Supabase). Live 26 (genuinamente nova, não
  # coberta pelo backfill de 14/08) processada normalmente: youtube_id sdaaCr63UX8, título
  # "26 - Ninguém te enxerga como autoridade e o problema não é sua competência", linha inserida
  # em encontros_mentoria (id 96f8d9b8-e125-4054-b733-8c324ed6127b). LIÇÃO PRA PRÓXIMA EXECUÇÃO:
  # antes de subir qualquer vídeo, checar se o título/data já existe em encontros_mentoria via
  # SELECT (não confiar só neste state.yaml) — mission logs anteriores e backfills podem ter
  # processado fora desta automação.

mentoria_em_grupo:
  ultimo_processado_titulo: "Encontro Incubadora - 2026/08/03 14:00 GMT-04:00 - Recording"
  ultimo_processado_data: "2026-08-03T22:34:41Z"  # createdTime do arquivo no Drive
  ultima_execucao: "2026-09-03"
  # 03/09/2026 (2ª rodada, missão #22, disparada via /course-publisher + weekly-sync.md direto):
  # reconfirmado — mesmo falso positivo do arquivo de 03/08 (precisão de timestamp), nenhuma
  # gravação nova real; "Encontro Incubadora (recurring)" continua sem existir na raiz nova.
  # 03/09/2026 (missão #21, disparo autônomo REGRA-015): reconfirmado — mesmo falso positivo do
  # arquivo de 03/08 (precisão de timestamp, já conhecido desde a missão #15), nenhuma gravação
  # nova real; "Encontro Incubadora (recurring)" continua sem existir na raiz nova.
  # 27/08/2026 (2ª rodada, missão #20, mesmo dia): reconfirmado de novo — mesmo falso positivo do
  # arquivo de 03/08 (precisão de timestamp, já conhecido desde a missão #15), nenhuma gravação
  # nova real; "Encontro Incubadora (recurring)" continua sem existir na raiz nova.
  # 27/08/2026 (missão #19): reconfirmado, mesmo resultado — só o arquivo de 03/08 já processado
  # (a busca com filtro de data ainda devolve esse mesmo arquivo por precisão de timestamp, não é
  # gravação nova; "Encontro Incubadora (recurring)" continua sem existir na raiz nova).
  # 13/08/2026: nenhuma gravação nova encontrada em nenhuma das duas raízes do Drive — não
  # houve Encontro Incubadora (ou não foi gravado) na segunda-feira 10/08.
  # 14/08/2026: a partir da PRÓXIMA gravação nova, destino vira YouTube+encontros_mentoria (não
  # mais Hotmart) — ver weekly-sync.md.
  # 20/08/2026: ainda nenhuma gravação nova em nenhuma das duas raízes (nem pasta clássica, nem
  # "Encontro Incubadora (recurring)" — essa subpasta continua sem existir na raiz nova).
  # 20/08/2026 (2ª rodada, missão #16, mesmo dia): reconfirmado, mesmo resultado.
  # 20/08/2026 (missão #17): reconfirmado de novo, mesmo resultado — só o arquivo de 03/08 já
  # processado nas duas raízes.

encontros_individuais:
  ultimo_processado_titulo: null
  ultimo_processado_data: null
  ultima_execucao: "2026-09-03"
  # 03/09/2026 (2ª rodada, missão #22, disparada via /course-publisher + weekly-sync.md direto):
  # reconfirmado — nenhum arquivo com prefixo "Individual -" em nenhuma raiz. Raiz "Google Meet"
  # segue só com pastas "Milena - ..." (nome da aluna + hora), mesmo achado da missão #17 —
  # decisão da Karol/Gestor de Infra Arcane sobre ajustar filtro ou renomear manualmente segue
  # pendente, não mexido aqui.
  # 03/09/2026 (missão #21, disparo autônomo REGRA-015): reconfirmado — nenhum arquivo com
  # prefixo "Individual -" em nenhuma raiz. Raiz "Google Meet" segue só com pastas "Milena - ..."
  # (nome da aluna + hora), mesmo achado da missão #17 — decisão da Karol/Gestor de Infra Arcane
  # sobre ajustar filtro ou renomear manualmente segue pendente, não mexido aqui.
  # 27/08/2026 (2ª rodada, missão #20, mesmo dia): reconfirmado — nenhum arquivo com prefixo
  # "Individual -" em nenhuma raiz. Achado da missão #17 (Meet nomeia pastas 1:1 pelo nome da
  # aluna, não pelo padrão combinado) segue pendente de decisão da Karol/Gestor de Infra Arcane.
  # 27/08/2026 (missão #19): reconfirmado — as pastas "Milena - ..." seguem sem o padrão
  # "Individual - {slug} - {AAAA-MM-DD}" esperado; nenhum arquivo novo com esse prefixo em
  # nenhuma raiz. Achado da missão #17 sobre ajuste de filtro/renomeação segue pendente de
  # decisão da Karol/Gestor de Infra Arcane — não mexido aqui.
  # 14/08/2026: série nova (padrão "Individual - {slug} - {AAAA-MM-DD}") — ainda nenhuma gravação
  # com esse padrão foi processada. Primeira rodada busca sem filtro de data mínima (campo null).
  # 20/08/2026: busca sem filtro de data repetida, nenhum arquivo com esse prefixo encontrado
  # em nenhuma das duas raízes.
  # 20/08/2026 (2ª rodada, missão #16, mesmo dia): reconfirmado, mesmo resultado.
  # 20/08/2026 (missão #17): reconfirmado de novo — a raiz "Google Meet" tem 2 pastas de sessão
  # 1:1 com a Milena ("Milena - 2026/08/19 15:02..." e "Milena 4/12 - 2026/08/05 15:01..."), mas
  # NENHUMA segue o padrão de nome "Individual - {slug} - {AAAA-MM-DD}" esperado pelo protocolo —
  # o Meet está nomeando pastas de 1:1 pelo nome da aluna + hora, não no padrão combinado no
  # briefing da Área de Membros (decisão 11/08). Filtro da busca não pega essas pastas (não
  # começam com "Individual -"). Não mexi em nada (fora do escopo desta missão) — registrar como
  # achado pra Karol/Gestor de Infra Arcane decidirem: ajustar o filtro do weekly-sync pra
  # reconhecer esse padrão novo, ou renomear as pastas manualmente. Sem isso, esta série nunca vai
  # processar nada mesmo que a Karol grave 1:1s.

pendencias: []
# 20/08/2026 (missão #17): pendência "acesso_drive_bloqueado" (registrada nas missões #15/#16)
# RESOLVIDA — Karol recompartilhou a pasta "Live Expert360 (recurring)" (confirmado: permissão
# "anyone:writer" apareceu via get_file_permissions, e o perfil karol.franzini@gmail.com acessou
# a pasta sem 403 no Playwright). Live 25 e Live 26 processadas nesta missão — ver notas na série
# live_expert360 acima pro achado do duplicado da Live 25 (já existia via backfill de 14/08).
